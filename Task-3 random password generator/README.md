Random Password Generator

📌 Project Description
This project is a **Random Password Generator** built using Python.  
It generates a strong password based on user preferences such as letters, numbers, and symbols.

---

🧠 Concepts Used

1. Python Basics
- Variables
- Input and Output statements
- Conditional statements (`if`, `else`)
- Loops (`while`, `for`)

2. Random Module
- `random.randint()` is used to generate random indexes
- Helps in creating unpredictable passwords

3. Strings and String Operations
- String concatenation
- String indexing
- Length checking

4. Lists
- Used to store randomly selected characters
- List operations like append and indexing

5. User Input Validation
- Checks whether the password length is valid
- Ensures at least one character type is selected

6. Logic Building
- Ensuring minimum one character from each selected category
- Shuffling characters to improve security

---

🛠 Technologies Used
- Python 3

---

▶️ How to Run the Program
1. Open terminal or command prompt  
2. Run the file using:
